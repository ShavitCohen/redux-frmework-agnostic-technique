{
  "name": "redux-middleware-technique",
  "private": true,
  "scripts": {
    "init": "yarn",
    "start": "echo 'try: yarn run react'",
    "react": "yarn --cwd ./implementations/react start",
    "vue": "yarn --cwd ./implementations/vue start"
  },
  "devDependencies": {},
  "postcss": {
    "plugins": {
      "autoprefixer": {}
    }
  },
  "workspaces": [
    "x-redux",
    "implementations/*"
  ],
  "dependencies": {
    "cwd": "^0.10.0"
  }
}
